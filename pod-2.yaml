# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-app-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-app-2
  template:
    metadata:
      labels:
        app: simple-app-2
    spec:
      containers:
      - name: simple-app-2
        image: pnsmiling/phong
        ports:
        - containerPort: 80
          name: simple-port-2
